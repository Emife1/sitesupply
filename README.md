# SiteSupply

SiteSupply is a premium construction supply quote workspace.

## What changed
- Split the prototype into `index.html`, `styles.css`, `data.js`, and `app.js`
- Preserved the visual language
- Rebuilt the app as a workspace-driven interface
- Added project, supplier, message, and quote flows

## Deployment
The app is intended for static hosting on Vercel.
